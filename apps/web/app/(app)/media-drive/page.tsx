import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Media Drive & Photo Vault | PartPulse",
  description: "Automated part-number folder scanning and image assignment for catalog parts",
};

/** Content is rendered by the shared workspace shell for instant sidebar navigation. */
export default function MediaDrivePage() {
  return null;
}
