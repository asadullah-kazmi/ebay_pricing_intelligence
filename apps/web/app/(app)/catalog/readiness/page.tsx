import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Publication Readiness | PartPulse",
  description: "Review and resolve eBay listing draft publication issues",
};

/** Content is rendered by the persistent Catalog workspace. */
export default function PublicationReadinessPage() {
  return null;
}
