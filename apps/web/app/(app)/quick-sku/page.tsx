import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Quick SKU | PartPulse",
  description: "Identify a part from OEM number and add it to your catalog with title, specs, and fitment",
};

/** Content is rendered by the shared workspace shell for instant sidebar navigation. */
export default function QuickSkuPage() {
  return null;
}
